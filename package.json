{
  "name": "toy-drone",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/copy-readme.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copy-readme.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "zip": "git archive --format=zip --prefix=toy-drone/ -o public/toy-drone-source.zip HEAD"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
